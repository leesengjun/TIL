# Flutter 8강 TIL

## 오늘 배운 내용

오늘은 `GridView.builder`를 사용해서 데이터를 격자 모양으로 화면에 출력하는 방법을 배웠다.

`GridView`는 `ListView`처럼 스크롤이 되는 위젯이지만, 한 줄로 나열하는 것이 아니라 여러 칸으로 나누어 보여준다.

## 핵심 개념

### GridView

`GridView`는 위젯들을 격자 형태로 배치하는 스크롤 위젯이다.

```text
[box 1] [box 2]
[box 3] [box 4]
[box 5] [box 6]
