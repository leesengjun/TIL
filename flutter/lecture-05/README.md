## 오늘 배운 내용

- `ListView`는 여러 위젯을 스크롤해서 보여줄 때 사용한다.
- `scrollDirection: Axis.horizontal`을 사용하면 가로 스크롤을 만들 수 있다.
- 반복되는 UI는 함수로 분리할 수 있다.
- 함수의 매개변수로 `title`, `colorData`를 받아서 글자와 색상을 다르게 만들 수 있다.
- `MediaQuery.of(context).size.width`를 사용하면 현재 화면의 너비를 가져올 수 있다.
